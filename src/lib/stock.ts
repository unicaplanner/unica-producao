// Constante pura (sem import de Prisma) pra poder ser usada tanto em
// Server Components quanto em Client Components.

// Variantes com menos de X unidades em estoque entram no alerta de
// estoque baixo.
export const LOW_STOCK_THRESHOLD = 2;
